-- Tabla permisos
CREATE TABLE permisos (
    id INT PRIMARY KEY AUTO_INCREMENT,
    nombre VARCHAR(100) NOT NULL UNIQUE,
    descripcion TEXT NULL
);

-- Tabla roles
CREATE TABLE roles (
    id INT PRIMARY KEY AUTO_INCREMENT,
    nombre VARCHAR(100) NOT NULL UNIQUE,
    descripcion TEXT NULL
);

-- Tabla intermedia roles_x_permiso (relación muchos a muchos)
CREATE TABLE roles_x_permiso (
    id INT PRIMARY KEY AUTO_INCREMENT,
    permiso_id INT NOT NULL,
    rol_id INT NOT NULL,
    FOREIGN KEY (permiso_id) REFERENCES permisos(id) ON DELETE CASCADE,
    FOREIGN KEY (rol_id) REFERENCES roles(id) ON DELETE CASCADE,
    UNIQUE KEY unique_permiso_rol (permiso_id, rol_id)
);

-- Tabla usuario
CREATE TABLE usuario (
    id INT PRIMARY KEY AUTO_INCREMENT,
    usuario VARCHAR(50) NOT NULL UNIQUE,
    correo VARCHAR(255) NOT NULL UNIQUE,
    passwd VARCHAR(255) NOT NULL,
    rol_id INT NOT NULL,
    fecha_creacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    FOREIGN KEY (rol_id) REFERENCES roles(id) ON DELETE RESTRICT
);

-- Índices adicionales para mejorar el rendimiento
CREATE INDEX idx_usuario_correo ON usuario(correo);
CREATE INDEX idx_roles_x_permiso_permiso ON roles_x_permiso(permiso_id);
CREATE INDEX idx_roles_x_permiso_rol ON roles_x_permiso(rol_id);
CREATE INDEX idx_usuario_rol ON usuario(rol_id);

-- 1. Tabla de categorías (para normalizar el campo categoría)
CREATE TABLE IF NOT EXISTS categorias (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(50) NOT NULL UNIQUE,
    descripcion TEXT,
    fecha_creacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 2. Tabla de autores (para normalizar el campo autor)
CREATE TABLE IF NOT EXISTS autores (
    id INT AUTO_INCREMENT PRIMARY KEY,
    nombre VARCHAR(100) NOT NULL,
    apellido VARCHAR(100),
    biografia TEXT,
    fecha_creacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

-- 3. Tabla principal de biblioteca (simplificada)
CREATE TABLE IF NOT EXISTS biblioteca (
    id INT AUTO_INCREMENT PRIMARY KEY,
    titulo VARCHAR(255) NOT NULL,
    enlace VARCHAR(500) NOT NULL,
    imagen VARCHAR(500) NULL,
    archivo_pdf VARCHAR(500) NULL,
    categoria_id INT,
    autor_id INT,
    fecha_creacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    fecha_actualizacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,
    FOREIGN KEY (categoria_id) REFERENCES categorias(id) ON DELETE SET NULL,
    FOREIGN KEY (autor_id) REFERENCES autores(id) ON DELETE SET NULL
);

-- 4. Insertar categorías predefinidas
INSERT IGNORE INTO categorias (nombre) VALUES 
('Filosofía'),
('Autoayuda'),
('Literatura'),
('Ciencia'),
('Historia');

-- 5. Crear índices para mejor rendimiento
CREATE INDEX IF NOT EXISTS idx_biblioteca_titulo ON biblioteca(titulo);
CREATE INDEX IF NOT EXISTS idx_biblioteca_categoria ON biblioteca(categoria_id);
CREATE INDEX IF NOT EXISTS idx_biblioteca_autor ON biblioteca(autor_id);
CREATE INDEX IF NOT EXISTS idx_autores_nombre ON autores(nombre, apellido);


-- Editar tabla usuario para agregar columna estado y fecha_modificacion
ALTER TABLE usuario 
ADD estado ENUM('Activo', 'Inactivo', 'Bloqueado') DEFAULT 'Activo',
ADD fecha_modificacion TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP;

-- Insertar roles predeterminados
INSERT INTO roles (nombre, descripcion) VALUES
('Administrador', 'Usuario con todos los permisos'),
('Editor', 'Usuario con permisos para editar contenido'),
('Visualizador', 'Usuario con permisos solo para ver contenido');

-- Insertar permisos predeterminados
INSERT INTO permisos (nombre, descripcion) VALUES
('usuario:Actualiza', 'Permiso para editar usuarios existentes'),
('usuario:Crea', 'Permiso para crear nuevos usuarios'),
('usuario:Elimina', 'Permiso para eliminar usuarios'),
('usuario:Lee', 'Permiso para ver detalles de usuarios'),
('rol:Crea', 'Permiso para crear nuevos roles'),
('rol:Actualiza', 'Permiso para editar roles existentes'),
('rol:Elimina', 'Permiso para eliminar roles'),
('rol:Lee', 'Permiso para ver detalles de roles'),
('permiso:Actualiza', 'Permiso para editar permisos existentes'),
('permiso:Crea', 'Permiso para crear nuevos permisos'),
('permiso:Elimina', 'Permiso para eliminar permisos'),
('permiso:Lee', 'Permiso para ver detalles de permisos'),
('biblioteca:Crea', 'Permiso para crear nuevos biblioteca/libros'),
('biblioteca:Actualiza', 'Permiso para editar biblioteca/libros existentes'),
('biblioteca:Elimina', 'Permiso para eliminar biblioteca/libros'),
('biblioteca:Lee', 'Permiso para ver detalles de biblioteca/libros'),
('categoria:Crea', 'Permiso para crear nuevas categorías'),
('categoria:Actualiza', 'Permiso para editar categorías existentes'),
('categoria:Elimina', 'Permiso para eliminar categorías'),
('categoria:Lee', 'Permiso para ver detalles de categorías'),
('autor:Crea', 'Permiso para crear nuevos autores'),
('autor:Actualiza', 'Permiso para editar autores existentes'),
('autor:Elimina', 'Permiso para eliminar autores'),
('autor:Lee', 'Permiso para ver detalles de autores'),
('dashboard:Lee', 'Permiso para ver el dashboard');

-- Asignar permisos a roles
-- Administrador tiene todos los permisos
INSERT INTO roles_x_permiso (permiso_id, rol_id)
SELECT p.id, r.id
FROM permisos p, roles r
WHERE r.nombre = 'Administrador';

-- Editor tiene permisos de usuario:Lee, usuario:Actualiza, rol:Lee
INSERT INTO roles_x_permiso (permiso_id, rol_id)
SELECT p.id, r.id
FROM permisos p, roles r
WHERE r.nombre = 'Editor' AND p.nombre IN ('usuario:Lee', 'usuario:Actualiza', 'rol:Lee');

-- Visualizador tiene solo permiso de usuario:Lee
INSERT INTO roles_x_permiso (permiso_id, rol_id)
SELECT p.id, r.id
FROM permisos p, roles r
WHERE r.nombre = 'Visualizador' AND p.nombre = 'usuario:Lee';

-- Insertar usuario administrador predeterminado (contraseña: ab)
INSERT INTO usuario (usuario, correo, passwd, rol_id) VALUES
('admin', 'admin@gmail.com', '$2y$10$H1qB1GSD.eWp1.vPv9WggOv5bVH/Y0ymf579jF/VbKDyl6Nk2mVS6', (SELECT id FROM roles WHERE nombre = 'Administrador'));